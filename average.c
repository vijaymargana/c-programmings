/*program for the average of the numbers*/
#include<stdio.h>
int main(){
	int y,z;
	float x;
	printf("enter the y value:");
	scanf("%d",&y);
	printf("enter the average of the two numbers:");
	scanf("%d",&x);
	z=2*x-y;
	printf("second number is %d",z);
	return 0;
}
