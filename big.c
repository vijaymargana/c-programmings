/*program for the big in the three numbers*/
#include<stdio.h>
int main(){
	int a,b;
	printf("enter the two numbers:");
	scanf("%d%d",&a,&b);
	if(a>b)
	{
		printf("%d is big in the number",a);
	}
	else
	{
		printf("%d is big in the number",b);
	}
}
