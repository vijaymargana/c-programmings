#include<stdio.h>
int mul(int,int);
int main()
{
	int a,b,mul;
	printf("enter the a,b value");
	scanf("%d%d",&a,&b);
	mul=a*b;
	printf("multiplication is:%d",mul);
	return 0;
}
int mul(int x,int y)
{
return 0;
}

